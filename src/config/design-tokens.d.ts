export const themeColors: {
  primary: string;
  accent: string;
  card: string;
  background: string;
  text: string;
  subText: string;
  border: string;
};

export const themeFontFamily: {
  sans: [string];
  medium: [string];
  bold: [string];
};
