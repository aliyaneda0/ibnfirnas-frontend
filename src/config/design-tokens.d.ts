export const themeColors: {
  navy: string;
  primary: string;
  primaryLight: string;
  sky: string;
  accent: string;
  error: string;
  surface: string;
  surfaceMuted: string;
  textPrimary: string;
  textMuted: string;
  whatsapp: string;
  secondary: string;
  card: string;
  background: string;
  text: string;
  textSecondary: string;
  border: string;
  success: string;
};

export const themeFontFamily: {
  sans: [string];
  medium: [string];
  bold: [string];
};
